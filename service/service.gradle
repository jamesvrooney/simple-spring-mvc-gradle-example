dependencies {
    compile project(':persistence')
    compile project(':model')

    compile 'org.springframework:spring-tx:' + springVersion
}

description = 'This is the service layer in the multi-project build.'